# CLAUDE.md

## プロジェクト概要

**melyuh-blog** はAstroで構築する個人技術ブログ兼ポートフォリオサイト。
「書類選考を突破するための技術的証拠」として機能させることを主目的とする。
AWS S3 + CloudFrontで静的配信し、Terraformでインフラを管理する。

## ドキュメント

**セッション開始時や実装を始める前** に以下を必ず読むこと。

1. [要件定義](.claude/specs/01-requirements.md) — 何を作るか・なぜ作るか
2. [アーキテクチャ設計](.claude/specs/02-architecture.md) — 技術スタック・デプロイ構成
3. [機能設計](.claude/specs/03-features.md) — 各ページの詳細仕様
4. [開発ガイドライン](.claude/specs/04-dev.md) — 規約・コマンド・フロー
5. [実装進捗](.claude/progress.md) — 現在の進捗・既知の課題

## セッション開始時のプロトコル

1. 上記ドキュメントをすべて読む
2. `git log main --oneline -20` を実行して最近のマージ済みコミットを確認する
3. `.claude/progress.md` の内容と差分があれば更新する
4. 現在のブランチと `.claude/tasklists/` の該当ファイルを確認して文脈を把握する

## クイックスタート

```bash
mise install   # Node.js / pnpm のバージョンをインストール
pnpm install   # 依存パッケージをインストール
pnpm dev       # 開発サーバーを起動
```

## ドキュメント更新ルール

機能変更・アーキテクチャ変更・開発フロー変更を行った際は、実装と同時に対応するドキュメントを必ず更新すること。

| 変更の種類 | 更新対象 |
|---|---|
| 要件・目標・ページ構成の変更 | 01-requirements.md |
| 技術スタック・デプロイ構成の変更 | 02-architecture.md |
| 機能仕様・フロントマターの変更 | 03-features.md |
| コマンド・規約・フローの変更 | 04-dev.md |
| 実装の完了・着手・既知の課題の変更 | progress.md |
| 制約・セッションルール・クイックスタートの変更 | CLAUDE.md |

## コミット・マージ時のルール

ユーザーがコミットまたはPRマージをしたと伝えたら（「コミットした」「マージした」等）、Claudeは即座に以下を実行すること：

1. `git log` で変更内容を確認する
2. `.claude/tasklists/` の該当ファイルのチェックボックスを更新する
3. `progress.md` の状態・最終更新日を更新する

## 作業単位のタスク管理（.claude/tasklists/）

機能実装・バグ修正など作業単位ごとに `.claude/tasklists/` にタスクファイルを作成する。

```
.claude/tasklists/YYYYMMDD-[作業タイトル].md
```

**ファイルの内容:**
- 今回の作業概要
- タスク一覧と進捗（`- [ ]` / `- [x]`）
- 完了条件

**運用ルール:**
- 作業開始時にClaude が作成する
- 作業中はタスク完了のたびに更新する
- 作業完了・マージ後もファイルは削除せず履歴として残す
- `progress.md` はマクロな進捗、`.claude/tasklists/` はミクロな作業単位、という2層で管理

## 主要な制約

- `infra/` 配下の `*.tfvars` と `*.tfstate` は `.gitignore`（機密情報のため）
- ブランチ戦略・UIテキスト規約・デプロイフローの詳細は `04-dev.md` を参照
